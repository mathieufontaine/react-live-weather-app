import React from "react";

const Spinner = () => {
  return (
    <img
      src={require("../../assets/img/spinner.gif")}
      alt="spinner"
      className="spinner"
    />
  );
};

export default Spinner;
